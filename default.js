// var fl = document.forms;

// fl.filter ;

// function likelysignup(f) {
//   var ret = f.action.match(/(register)|(signup)/);
//   ret = ret || f;
// }
// TODO: insert a hook so that I can have rescuetime-like tracking functionality 
